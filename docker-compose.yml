services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
      - DJANGO_SECRET_KEY=change-me-in-production

  frontend:
    build: ./frontend
    ports:
      - "80:80"
    depends_on:
      - backend
